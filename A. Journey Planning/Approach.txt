If we take the given equation and rearrange it, we get -> c(i+1)-b(c(i+1))=c(i)-b(c(i)). So we can just calculate this value for each element of the array, and divide them into buckets based on it. All the elements in a bucket could be a valid path. So the maximum sum of elements out of all the buckets will be our final answer. We can use a hashmap to divide the elements into the buckets.
